#include <stdio.h>
/**
* main - print from A to Z
*
* Return: Always 0 (Success)
*/
int main(void)
{
char x;
for (x <= 'z' ; x = 'a'; x--)
{
putchar(x);
}
for (x = 'A' ; x <= 'Z'; x++)
{
putchar(x);
}
putchar('\n');
return (0);
}
